require "sinatra/activerecord/rake"


# ActiveRecord::Base.establish_connection(
#     :adapter => 'sqlite3',
#     :database => 'db.sqlite3'
#   )

ActiveRecord::Base.establish_connection(
    :adapter => 'postgresql',
    :database => 'DATABASE_URL'
  )
